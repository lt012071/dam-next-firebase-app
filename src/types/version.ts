export interface Version {
  id: string;
  assetId: string;
  version: string;
  fileUrl: string;
  fileName: string;
  fileType: string;
  fileSize: number;
  updatedAt: string;
  updatedBy: string;
  // 必要に応じて追加
} 